{% extends 'base.html.twig' %}

{% block body %}
    <h3>  Les sorties </h3>
        {% for chaquesortie in sorties %}
            <div>{{ chaquesortie.name}} </div>
            <div>{{ chaquesortie.lieu.nom}} </div>
            <div>{{ chaquesortie.infosSortie}} </div>


        {% endfor %}

{% endblock %}
